# Nuvia Backend API

Backend API для интернет-магазина Nuvia на базе NestJS, Prisma и PostgreSQL.

## 🚀 Технологии

- **NestJS** - прогрессивный Node.js фреймворк
- **Prisma** - современная ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **AWS S3 / Cloudflare R2** - хранилище файлов
- **JWT** - аутентификация
- **Swagger** - документация API
- **class-validator** - валидация данных

## 📋 Требования

- Node.js >= 18.x
- PostgreSQL >= 14.x
- npm или yarn

## 🔧 Установка

1. **Клонируйте репозиторий и перейдите в папку проекта:**

```bash
cd nuvia-back
```

2. **Установите зависимости:**

```bash
npm install
```

3. **Настройте переменные окружения:**

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните следующие переменные:

```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/nuvia_db?schema=public"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="24h"

# AWS S3 (или Cloudflare R2)
AWS_ACCESS_KEY_ID="your-access-key"
AWS_SECRET_ACCESS_KEY="your-secret-key"
AWS_REGION="us-east-1"
AWS_S3_BUCKET_NAME="nuvia-uploads"
AWS_S3_ENDPOINT=""
# Для R2 используйте: AWS_S3_ENDPOINT="https://<account-id>.r2.cloudflarestorage.com"

# Server
PORT=3000
NODE_ENV=development
```

4. **Создайте базу данных PostgreSQL:**

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE nuvia_db;
```

5. **Запустите миграции Prisma:**

```bash
# Генерируем Prisma Client
npm run prisma:generate

# Применяем миграции (автоматически запустит seed)
npm run prisma:migrate

# Или запустите seed вручную
npm run prisma:seed
```

Seed скрипт создаст первого администратора:
- Email: `admin@nuvia.com` (или из переменной окружения `ADMIN_EMAIL`)
- Password: `admin123` (или из переменной окружения `ADMIN_PASSWORD`)

А также создаст примеры категорий (mugs, t-shirts, keychains).

6. **Запустите приложение:**

```bash
# Режим разработки
npm run start:dev

# Продакшн режим
npm run build
npm run start:prod
```

## 📚 API Документация

После запуска приложения, Swagger документация будет доступна по адресу:

```
http://localhost:3000/docs
```

## 🔐 API Endpoints

### Auth

- `POST /auth/login` - Вход в систему (публичный)
- `POST /auth/register` - Регистрация нового администратора (требует авторизации)

### Products

- `GET /products` - Получить все товары (публичный)
- `GET /products/:id` - Получить товар по ID (публичный)
- `POST /products` - Создать товар (требует авторизации админа)
- `PATCH /products/:id` - Обновить товар (требует авторизации админа)
- `DELETE /products/:id` - Удалить товар (требует авторизации админа)

### Categories

- `GET /categories` - Получить все категории (публичный)
- `GET /categories/:id` - Получить категорию по ID (публичный)
- `POST /categories` - Создать категорию (требует авторизации админа)
- `PATCH /categories/:id` - Обновить категорию (требует авторизации админа)
- `DELETE /categories/:id` - Удалить категорию (требует авторизации админа)

### Uploads

- `POST /upload` - Загрузить один файл на S3 (требует авторизации админа)
- `POST /upload/multiple` - Загрузить несколько файлов на S3 (требует авторизации админа)

## 🔑 Авторизация

Для доступа к защищенным эндпоинтам необходимо:

1. Выполнить вход через `POST /auth/login` с email и password
2. Получить `access_token` из ответа
3. Использовать токен в заголовке запроса: `Authorization: Bearer <access_token>`

Пример:

```bash
curl -X POST http://localhost:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@nuvia.com", "password": "password123"}'
```

## 📁 Структура проекта

```
src/
├── auth/              # Модуль аутентификации
│   ├── dto/          # Data Transfer Objects
│   ├── strategies/   # JWT стратегия
│   ├── auth.controller.ts
│   ├── auth.service.ts
│   └── auth.module.ts
├── products/         # Модуль товаров
│   ├── dto/
│   ├── products.controller.ts
│   ├── products.service.ts
│   └── products.module.ts
├── categories/       # Модуль категорий
│   ├── dto/
│   ├── categories.controller.ts
│   ├── categories.service.ts
│   └── categories.module.ts
├── uploads/          # Модуль загрузки файлов
│   ├── uploads.controller.ts
│   ├── uploads.service.ts
│   └── uploads.module.ts
├── prisma/           # Prisma сервис
│   ├── prisma.service.ts
│   └── prisma.module.ts
├── common/           # Общие утилиты
│   ├── config/       # Конфигурация (S3)
│   ├── decorators/   # Декораторы (Public)
│   └── guards/       # Guards (JWT, Admin)
├── app.module.ts     # Главный модуль
└── main.ts           # Точка входа
```

## 🗄️ База данных

### Модели Prisma

#### User
- `id` - уникальный идентификатор
- `email` - email адрес (уникальный)
- `password` - хешированный пароль
- `role` - роль пользователя (по умолчанию 'admin')
- `createdAt` - дата создания
- `updatedAt` - дата обновления

#### Category
- `id` - уникальный идентификатор
- `slug` - уникальный slug
- `nameEn`, `nameRu`, `nameArm` - названия на трех языках
- `createdAt` - дата создания
- `updatedAt` - дата обновления

#### Product
- `id` - уникальный идентификатор
- `categoryId` - ID категории (связь с Category)
- `nameEn`, `nameRu`, `nameArm` - названия на трех языках
- `descriptionEn`, `descriptionRu`, `descriptionArm` - описания на трех языках
- `price` - цена (в копейках/центах)
- `image` - основное изображение (URL)
- `images` - массив дополнительных изображений (URL[])
- `sliderDescriptionEn`, `sliderDescriptionRu`, `sliderDescriptionArm` - описания для слайдера
- `filters` - фильтры в формате JSON
- `createdAt` - дата создания
- `updatedAt` - дата обновления

## 🛠️ Полезные команды

```bash
# Разработка
npm run start:dev          # Запуск в режиме разработки с hot-reload

# База данных
npm run prisma:generate    # Генерация Prisma Client
npm run prisma:migrate     # Применение миграций
npm run prisma:studio      # Открыть Prisma Studio (GUI для БД)

# Сборка
npm run build              # Сборка проекта
npm run start:prod         # Запуск в продакшн режиме

# Линтинг и форматирование
npm run lint               # Проверка кода
npm run format             # Форматирование кода
```

## 🔒 Безопасность

- Пароли хранятся в хешированном виде (bcrypt)
- JWT токены имеют срок действия
- Валидация всех входящих данных через class-validator
- CORS настроен для безопасности
- Защищенные эндпоинты требуют JWT токен и роль администратора

## 📝 Создание первого администратора

После запуска миграций, создайте первого администратора через API:

```bash
# Сначала нужно временно убрать проверку админа для регистрации
# Или использовать Prisma Studio для создания пользователя вручную
```

Или через Prisma Studio:

```bash
npm run prisma:studio
```

## 🌐 Настройка S3 / R2

### AWS S3

1. Создайте bucket в AWS S3
2. Настройте публичный доступ (если нужно)
3. Получите Access Key ID и Secret Access Key
4. Заполните переменные окружения

### Cloudflare R2

1. Создайте R2 bucket в Cloudflare
2. Получите Access Key ID и Secret Access Key
3. Установите `AWS_S3_ENDPOINT` в формате: `https://<account-id>.r2.cloudflarestorage.com`
4. Заполните остальные переменные окружения

## 🐛 Решение проблем

### Ошибка подключения к базе данных

Убедитесь, что:
- PostgreSQL запущен
- DATABASE_URL правильный
- База данных создана

### Ошибка загрузки файлов

Проверьте:
- Правильность AWS credentials
- Существование S3 bucket
- Правильность настроек endpoint (для R2)

### Ошибки миграций

```bash
# Сброс базы данных (ОСТОРОЖНО: удалит все данные)
npx prisma migrate reset

# Применение миграций заново
npm run prisma:migrate
```

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

Для разработки рекомендуется использовать:
- VS Code с расширениями для NestJS и Prisma
- Postman или Insomnia для тестирования API
- Prisma Studio для просмотра данных

---

**Приятной разработки! 🚀**

